task1:
issue: missing audit trails for absences, absences-balance tracking.
fix: add audit trails for missing app modules. Ensure logs cover: events, data updates, approvals, submissions, deletions and
other logable information.

task2:
issue: employee/user not being supervisor or admin or superuser can manualy create auto-approved absence for himself and for other
 employees. When created for himself. in 'moje nieobecnosci' page it is listed in the table with 'przełożony' = himself (which is
 illogical).
fix:
 udpate persmissions management so that each toggleabe persmission covers narrow and specific are of application
 business-logic; expected implementation result: more permissions in the selected-role-permission-edit page-view that can be
 switched on/off.
 for each persmission unit add:
  ** 'read-only' on/off switch, which when on - the permission is limited to only view the data but cannot make
  chages/udpates/add-create (edit-create-form-views not accessible/disabled/not-displayed; clickable elements disabled).
  ** 'own-data' on/off switch, which when on - the user/employee can only see its own data/information displayed in lists, table,
  views etc.

