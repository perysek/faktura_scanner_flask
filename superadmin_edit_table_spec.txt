To add:
superadmin_edit_table.html with editable table view, each row action-column with action icon btns
Each cell must be editable in-line without switching to different page view (except modal for adding/editing/removing services as in #2)

Spec:
#1 table columns same as input fields in superadmin_edit.html
#2 each colum must have same type input fields as superadmin_edit.html (text box, dropdowns, custome-desing modal window for adding/editing price and duration/removing services to appointment - route via acion button)
#3 each dropdown must be populated with data same way as in superadmin_edit
#4 each dropdown must have real-time-search feature added
#5 table must have customer rating column before action buttons column - displayed as 5-star clickable icons
#6 table view must have navigation keys implemented to allow easy and fast switching between row column edits, switching to next/previous table row
#7 edit table view to be added to side nav
#8 appointment conflict triggered on each row update
#9 save changes as action button (disabled if no change made in appointment row)
#10 appointment conflict check on each row save
#11 styling, layout, grids and js features etc. to be consistent with current GUI desing already used.
#12 table full viewport width and height will full remaining available height. on scroll data loading, each column header with sorting dynamic icons buttons and realtime search filter input field with filter reset icon button per each.
#13 added main 'reset filters' button above table wrapper.
#14 table with vertical scroll bar
#15 no horisontal scroll - all columns visible and responsive widths with % allowing to show data rows. wrap in word-break for each cell.


TASK: add side bar section 'KOREKTA DANYCH' and move there existing items: 'admin wizyty' and 'admin tabela' from 'system' section. Rename to: Edycja wizyt, Tabela wizyt - respectively. add new module 'Korekta danych' with toggle switch to roles/edit view.


SELECT workers.id, workers.firstname, workers.surname, workers.job_id, workers.boss_id, workers.gender, workers.hire_date, workers.fire_date
FROM workers workers