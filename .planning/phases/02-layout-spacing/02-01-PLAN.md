---
phase: 02-layout-spacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
  - templates/analytics/dashboard.html
autonomous: false
requirements:
  - SPAC-01
must_haves:
  truths:
    - "base.html #main-content no longer applies any padding — it is p-0"
    - "analytics/dashboard.html content has padding (1rem 1.5rem) after the base change"
    - "No page renders flush against the viewport edge due to this wave"
  artifacts:
    - path: "templates/base.html"
      provides: "Padding-free #main-content base"
      contains: "flex-1 overflow-auto p-0"
    - path: "templates/analytics/dashboard.html"
      provides: "Self-owned padding on content wrapper"
      contains: "padding: 1rem 1.5rem"
  key_links:
    - from: "templates/base.html"
      to: "all templates"
      via: "Jinja2 {% extends %}"
      pattern: "p-0.*id=\"main-content\""
    - from: "templates/analytics/dashboard.html"
      to: "first div in {% block content %}"
      via: "inline style or {% block extra_css %}"
      pattern: "padding.*1rem 1.5rem"
---

<objective>
Change base.html so #main-content applies zero padding (p-0), then immediately restore padding on analytics/dashboard.html which has no wrapper of its own and will otherwise render flush to the viewport edge.

Purpose: Eliminates the root cause of all 13 !important padding fights. Every template that already owns its padding via .refined-page or inline wrapper is unaffected. analytics/dashboard.html is the single template that currently relies on base.html p-2 with no local override — it must be patched in the same wave.

Output: base.html with p-0, analytics/dashboard.html with self-owned padding, zero visual regression on all pages that already have wrappers.
</objective>

<execution_context>
@C:/Users/piotrperesiak/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/piotrperesiak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-spacing/02-CONTEXT.md
@.planning/phases/02-layout-spacing/02-RESEARCH.md

<interfaces>
<!-- Key facts extracted from codebase. No exploration needed. -->

From templates/base.html line 44 (EXACT current state):
```html
<main class="flex-1 overflow-auto p-2" id="main-content">
```
Target (only change the Tailwind class p-2 → p-0, nothing else):
```html
<main class="flex-1 overflow-auto p-0" id="main-content">
```

From templates/analytics/dashboard.html lines 1-6 (EXACT current state):
```html
{% extends "base.html" %}
{% block title %}Analityka - {{ app_name }}{% endblock %}

{% block content %}
<!-- Page Header -->
<div class="mb-6">
```
This template has NO {% block extra_css %}, NO #main-content override, NO .refined-page class.
It currently relies on base.html p-2 for all its padding. After p-0 it will be flush.

Required addition — add {% block extra_css %} BEFORE {% block content %}:
```html
{% block extra_css %}
<style>
#main-content {
    background: var(--color-surface-warm);
}
.analytics-page {
    padding: 1rem 1.5rem;
}
</style>
{% endblock %}

{% block content %}
<div class="analytics-page">
  <!-- existing content unchanged -->
</div>
{% endblock %}
```
The outermost div in {% block content %} must receive class="analytics-page" (or equivalent inline style).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Change base.html #main-content from p-2 to p-0</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html (full file — especially line 44)
  </read_first>
  <action>
    On line 44 of templates/base.html, change the Tailwind class from p-2 to p-0:

    BEFORE:
    ```html
    <main class="flex-1 overflow-auto p-2" id="main-content">
    ```

    AFTER:
    ```html
    <main class="flex-1 overflow-auto p-0" id="main-content">
    ```

    This is a one-character diff. Do not touch any other line in base.html. Do not change the footer, sidebar, or any other element.

    The change is safe because every template that needs padding already overrides it via:
    - .refined-page class in {% block extra_css %} (appointments, employees, services, income, calendar, roles, users, clients/*)
    - Inline style on the first content div (clients/list, clients/view)
    - #main-content { padding: X !important } — these templates are handled in Plan 02 Wave 2

    The only exception (analytics/dashboard.html) is handled in Task 2 of this plan.
  </action>
  <verify>
    <automated>grep -n "p-0\|p-2" /c/Projects/faktura_scanner_flask/templates/base.html | grep "main-content\|flex-1"</automated>
    Expected output shows "p-0" on the #main-content line, not "p-2".
  </verify>
  <acceptance_criteria>
    - grep -n "p-2" templates/base.html returns NO match on the #main-content line
    - grep -n "p-0" templates/base.html returns the main-content line
    - File is otherwise unchanged (line count difference: 0)
  </acceptance_criteria>
  <done>base.html #main-content carries p-0 Tailwind class. One line changed, nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Restore padding on analytics/dashboard.html after base.html p-0</name>
  <files>templates/analytics/dashboard.html</files>
  <read_first>
    - templates/analytics/dashboard.html (full file — confirm no existing {% block extra_css %}, check outermost div in {% block content %})
  </read_first>
  <action>
    analytics/dashboard.html currently has no {% block extra_css %} and no .refined-page or similar wrapper. It will be flush against the viewport after Task 1. Fix:

    1. Add {% block extra_css %} immediately before {% block content %} (currently line 4):
    ```html
    {% block extra_css %}
    <style>
    #main-content {
        background: var(--color-surface-warm);
    }
    .analytics-page {
        padding: 1rem 1.5rem;
    }
    </style>
    {% endblock %}
    ```

    2. Wrap the existing content in {% block content %} inside a div with class="analytics-page":
    ```html
    {% block content %}
    <div class="analytics-page">
    <!-- ALL existing content from the original {% block content %} here, untouched -->
    </div>
    {% endblock %}
    ```

    The .analytics-page class is full-width (no max-width) — analytics/dashboard is a full-width page per SPAC-02 and CONTEXT.md decision. Do not add max-width. Only add padding: 1rem 1.5rem.

    Do not move, reorder, or modify any existing HTML inside {% block content %}. Only wrap it.
  </action>
  <verify>
    <automated>grep -n "analytics-page\|padding.*1rem" /c/Projects/faktura_scanner_flask/templates/analytics/dashboard.html</automated>
    Expected: shows .analytics-page with padding: 1rem 1.5rem
  </verify>
  <acceptance_criteria>
    - grep -n "analytics-page" templates/analytics/dashboard.html returns at least 2 hits (style definition + class usage)
    - grep -n "padding.*1rem 1.5rem" templates/analytics/dashboard.html returns at least 1 hit
    - grep -n "max-width" templates/analytics/dashboard.html returns NO hit in the new analytics-page rule (media queries for existing elements are OK)
    - File has {% block extra_css %} block that did not exist before
  </acceptance_criteria>
  <done>analytics/dashboard.html has self-owned padding (1rem 1.5rem) via .analytics-page wrapper. Content is full-width, no max-width added. No existing markup modified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual spot-check — Wave 1 regression check (5 key pages)</name>
  <what-built>Changed base.html p-2 → p-0 (affects all pages) and added padding wrapper to analytics/dashboard.html. All other pages that already had .refined-page or inline-style wrappers should be visually identical.</what-built>
  <how-to-verify>
    Run the Flask dev server: flask run

    Open each page and confirm visual appearance is correct:

    1. http://localhost:5000/dashboard — Should show content with normal padding (not flush to edge). Stats grid should look identical to before.
    2. http://localhost:5000/invoices — (invoices/list_refined) Should look identical to before — this template has !important which still wins until Plan 02 strips it.
    3. http://localhost:5000/clients — (clients/list) Should look identical to before — has inline style wrapper.
    4. http://localhost:5000/appointments/calendar — Should look identical to before — has .refined-page max-width 1400px which provides padding.
    5. http://localhost:5000/analytics — Should show content with padding ~1rem from edges (not flush). Previously used base.html p-2; now uses .analytics-page padding. Content arrangement should be unchanged.

    The test for page 5 (analytics) is the critical one — it's the only page that actively changed padding source.

    Any page appearing flush to the viewport edge (content touching the sidebar/top) = regression.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 pages look correct, or describe any issues</resume-signal>
  <files>templates/base.html, templates/analytics/dashboard.html</files>
  <action>Human verification step � run flask run and visually inspect the 5 listed pages. No code changes made in this task.</action>
  <verify><automated>N/A � human visual check required</automated></verify>
  <done>All 5 key pages render with correct padding and no visual regressions from the base.html p-0 change.</done>
</task>

</tasks>

<verification>
grep -n "p-0\|p-2" templates/base.html | grep "flex-1"
Expected: shows p-0

grep -rn "padding.*!important" templates/ --include="*.html" | wc -l
Expected: 13 (unchanged — !important cleanup happens in Plan 02)

grep -n "analytics-page" templates/analytics/dashboard.html | wc -l
Expected: at least 2
</verification>

<success_criteria>
- templates/base.html line 44: class="flex-1 overflow-auto p-0" (p-2 removed)
- templates/analytics/dashboard.html: has {% block extra_css %} with .analytics-page { padding: 1rem 1.5rem } and a wrapping div in {% block content %}
- Human visual check passes for all 5 key pages
- No other files modified in this wave
</success_criteria>

<output>
After completion, create .planning/phases/02-layout-spacing/02-01-SUMMARY.md
</output>
