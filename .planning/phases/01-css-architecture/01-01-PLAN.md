---
phase: 01-css-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/css/input.css
  - static/css/output.css
autonomous: true
requirements:
  - TYPO-01

must_haves:
  truths:
    - "input.css contains a global .page-title definition at font-size: 1.75rem with font-family: var(--font-display)"
    - "input.css contains a global .stat-value definition at font-size: 1.25rem"
    - "input.css contains a global .page-subtitle definition at font-size: 0.8125rem"
    - "input.css contains a global .stat-label definition at font-size: 0.6875rem"
    - "npm run build:css exits 0 after the addition"
  artifacts:
    - path: "static/css/input.css"
      provides: "Global typography component classes inside @layer components"
      contains: ".page-title {"
    - path: "static/css/output.css"
      provides: "Compiled CSS served to all templates"
  key_links:
    - from: "static/css/input.css"
      to: "static/css/output.css"
      via: "npm run build:css"
      pattern: "tailwindcss -i"
---

<objective>
Add a single canonical TYPOGRAPHY block inside `@layer components` in `static/css/input.css` that defines the four shared typography classes: `.page-title`, `.page-subtitle`, `.stat-value`, `.stat-label`. Run the build so `output.css` (served to templates) reflects the new definitions.

Purpose: This is the foundation for Wave 2 cleanup — the global definitions must exist in `output.css` before any template strips its local copy.
Output: `input.css` with typography block appended inside `@layer components`; fresh `output.css`.
</objective>

<execution_context>
@C:/Users/piotrperesiak/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/piotrperesiak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-architecture/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append global typography block to @layer components in input.css</name>
  <files>static/css/input.css</files>
  <read_first>
    - static/css/input.css — read in full to locate the closing `}` of `@layer components` (currently line 690); the new block must be inserted BEFORE that closing brace, not after it
  </read_first>
  <action>
Insert the following CSS block at the END of the `@layer components { ... }` block in `static/css/input.css`, immediately before the final closing `}` of that layer (currently at line 690). Do NOT add it after the layer closes.

Exact CSS to insert:

```css
  /* ============================================
     TYPOGRAPHY — Global shared classes
     Phase 1: CSS Architecture (TYPO-01, TYPO-02, TYPO-03)
     ============================================ */

  .page-title {
      font-family: var(--font-display);
      font-size: 1.75rem;
      font-weight: 600;
      letter-spacing: -0.02em;
      color: var(--color-ink);
  }

  .page-subtitle {
      color: var(--color-ink-muted);
      font-size: 0.8125rem;
      font-weight: 300;
  }

  .stat-value {
      font-family: var(--font-display);
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--color-ink);
      line-height: 1;
  }

  .stat-label {
      font-size: 0.6875rem;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      color: var(--color-ink-subtle);
  }
```

Critical notes:
- `.page-title` uses `font-size: 1.75rem` (NOT 1.5rem). This is the unified target per user decision.
- `.stat-value` uses `font-size: 1.25rem` (NOT 1.5rem or 1.75rem). This is the unified target per user decision.
- `.page-title` does NOT include `margin-bottom` — margin stays per-template context.
- `color: var(--color-ink)` IS included in `.page-title` (defensive — body already sets it, but explicit is cleaner and allows safe removal from templates).
- `.stat-value` includes `font-family: var(--font-display)` so all stat numbers use the display font.
  </action>
  <verify>
    <automated>grep -n "font-size: 1.75rem" C:/Projects/faktura_scanner_flask/static/css/input.css && grep -n "font-size: 1.25rem" C:/Projects/faktura_scanner_flask/static/css/input.css && grep -n "font-family: var(--font-display)" C:/Projects/faktura_scanner_flask/static/css/input.css | grep -c "font-family"</automated>
  </verify>
  <done>
    - `static/css/input.css` contains `.page-title { font-family: var(--font-display); font-size: 1.75rem; ...` inside `@layer components`
    - `static/css/input.css` contains `.stat-value { font-family: var(--font-display); font-size: 1.25rem; ...` inside `@layer components`
    - `static/css/input.css` contains `.page-subtitle { ... font-size: 0.8125rem; ...` inside `@layer components`
    - `static/css/input.css` contains `.stat-label { font-size: 0.6875rem; ...` inside `@layer components`
    - File structure is valid: `@layer components { ... }` block has exactly one closing `}` at the end
  </done>
</task>

<task type="auto">
  <name>Task 2: Run build and verify output.css reflects new classes</name>
  <files>static/css/output.css</files>
  <read_first>
    - static/css/input.css — confirm the typography block was added correctly before running build
  </read_first>
  <action>
Run the Tailwind build to regenerate `output.css` from the updated `input.css`:

```bash
cd C:/Projects/faktura_scanner_flask && npm run build:css
```

The command is: `tailwindcss -i ./static/css/input.css -o ./static/css/output.css --minify`

This command must exit 0. If it fails, check `input.css` for syntax errors (mismatched braces, typos in property names).

Do NOT edit `output.css` directly — it is fully regenerated by the build command.
  </action>
  <verify>
    <automated>cd C:/Projects/faktura_scanner_flask && npm run build:css && echo "BUILD_OK" && grep -c "page-title" static/css/output.css</automated>
  </verify>
  <done>
    - `npm run build:css` exits 0 with no errors
    - `static/css/output.css` contains `.page-title` class definition (grep returns count > 0)
    - `static/css/output.css` is updated (file modification timestamp newer than before build)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# 1. Confirm all 4 classes present in input.css
grep -n "\.page-title\|\.page-subtitle\|\.stat-value\|\.stat-label" C:/Projects/faktura_scanner_flask/static/css/input.css

# 2. Confirm build succeeds
cd C:/Projects/faktura_scanner_flask && npm run build:css

# 3. Confirm output.css contains the compiled definitions
grep -c "page-title" C:/Projects/faktura_scanner_flask/static/css/output.css
```

Expected: `grep` returns 4+ lines, build exits 0, output.css has page-title count > 0.
</verification>

<success_criteria>
- `static/css/input.css` has the 4-class typography block inside `@layer components` with exact values: `.page-title` at 1.75rem, `.stat-value` at 1.25rem, `.page-subtitle` at 0.8125rem, `.stat-label` at 0.6875rem
- `npm run build:css` exits 0
- `static/css/output.css` is regenerated and contains the compiled typography classes
- No existing `@layer components` rules were deleted or modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-architecture/01-01-SUMMARY.md`
</output>
