---
phase: 01-css-architecture
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - templates/appointments/calendar.html
  - templates/appointments/calendar_month.html
  - templates/appointments/calendar_week.html
  - templates/appointments/create.html
  - templates/appointments/edit.html
  - templates/appointments/list.html
  - templates/appointments/view.html
  - templates/auth/change_password.html
  - templates/clients/create.html
  - templates/clients/edit.html
  - templates/clients/list.html
  - templates/clients/view.html
  - templates/dashboard/index.html
  - templates/employees/create.html
  - templates/employees/edit.html
  - templates/employees/formy_zatrudnienia/list.html
  - templates/employees/list.html
  - templates/employees/view.html
  - templates/history/list_refined.html
  - templates/income/dashboard.html
  - templates/invoices/create.html
  - templates/invoices/edit.html
  - templates/invoices/list_refined.html
  - templates/invoices/upload.html
  - templates/roles/create.html
  - templates/roles/edit.html
  - templates/roles/list.html
  - templates/sellers/create.html
  - templates/sellers/edit.html
  - templates/sellers/list_refined.html
  - templates/services/create.html
  - templates/services/edit.html
  - templates/services/list.html
  - templates/services/view.html
  - templates/settings/email.html
  - templates/users/create.html
  - templates/users/edit.html
  - templates/users/list.html
autonomous: true
requirements:
  - TYPO-01
  - TYPO-02

must_haves:
  truths:
    - "Navigating from dashboard to calendar_month shows .page-title at the same visual size (both 1.75rem via global)"
    - "No template file contains a full .page-title redeclaration (only delta overrides like margin-bottom are acceptable)"
    - "No template file contains a full .page-subtitle redeclaration (only non-standard deltas like font-family: monospace or different font-size are acceptable)"
    - "calendar_month.html and calendar_week.html no longer override font-size: 1.375rem"
  artifacts:
    - path: "templates/appointments/calendar_month.html"
      provides: "Calendar month view without local .page-title override"
    - path: "templates/appointments/calendar_week.html"
      provides: "Calendar week view without local .page-title override"
  key_links:
    - from: "templates/*.html (all)"
      to: "static/css/output.css"
      via: "link rel=stylesheet in base.html"
      pattern: "output.css"
---

<objective>
Strip local `.page-title` and `.page-subtitle` CSS block redeclarations from all 38 Jinja2 templates. After Plan 01 added global definitions to `output.css`, local blocks that fully duplicate those definitions are now dead weight — worse, they silently override the global value for any template that still declares a different font-size.

Purpose: TYPO-01 (no duplicates) and TYPO-02 (uniform 1.75rem on all pages including calendars).
Output: All templates either have no `.page-title` / `.page-subtitle` block OR retain only a single-property delta override (margin-bottom, margin: 0, line-height, etc.).
</objective>

<execution_context>
@C:/Users/piotrperesiak/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/piotrperesiak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-css-architecture/01-RESEARCH.md
@.planning/phases/01-css-architecture/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Global typography definitions now in output.css (added by Plan 01). -->
<!-- Templates can safely delete full redeclarations — the global handles it. -->
<!-- Local <style> tags in <head> belong to the implicit outer layer, which -->
<!-- has HIGHER specificity than @layer components. This means a remaining -->
<!-- local block WILL still override the global — so removal is mandatory.  -->

Global definitions now in effect (from static/css/input.css @layer components):
  .page-title     { font-family: var(--font-display); font-size: 1.75rem; font-weight: 600; letter-spacing: -0.02em; color: var(--color-ink); }
  .page-subtitle  { color: var(--color-ink-muted); font-size: 0.8125rem; font-weight: 300; }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Strip local .page-title blocks from all templates</name>
  <files>
    templates/appointments/calendar.html,
    templates/appointments/calendar_month.html,
    templates/appointments/calendar_week.html,
    templates/appointments/create.html,
    templates/appointments/edit.html,
    templates/appointments/list.html,
    templates/appointments/view.html,
    templates/auth/change_password.html,
    templates/clients/create.html,
    templates/clients/edit.html,
    templates/clients/list.html,
    templates/clients/view.html,
    templates/dashboard/index.html,
    templates/employees/create.html,
    templates/employees/edit.html,
    templates/employees/formy_zatrudnienia/list.html,
    templates/employees/list.html,
    templates/employees/view.html,
    templates/history/list_refined.html,
    templates/income/dashboard.html,
    templates/invoices/create.html,
    templates/invoices/edit.html,
    templates/invoices/list_refined.html,
    templates/invoices/upload.html,
    templates/roles/create.html,
    templates/roles/edit.html,
    templates/roles/list.html,
    templates/sellers/create.html,
    templates/sellers/edit.html,
    templates/sellers/list_refined.html,
    templates/services/create.html,
    templates/services/edit.html,
    templates/services/list.html,
    templates/services/view.html,
    templates/settings/email.html,
    templates/users/create.html,
    templates/users/edit.html,
    templates/users/list.html
  </files>
  <read_first>
    Read each file being modified before editing. Priority reads (these have special cases):
    - templates/appointments/calendar_month.html (line 9: single-line block with margin-bottom: 0.25rem — keep margin-bottom as delta)
    - templates/appointments/calendar_week.html (line 9: same as calendar_month.html)
    - templates/dashboard/index.html (lines 41-46: has `margin: 0` — keep as delta)
    - templates/invoices/list_refined.html (line 39-47: has `margin: 0` and `line-height: 1.2` — keep as delta; line 636: `@media (max-width: 1024px) { .page-title { font-size: 1.25rem; } }` — KEEP this media query, do NOT remove it)
    - templates/invoices/create.html (lines 27-33: has `margin: 0` — keep as delta)
    - templates/invoices/upload.html (lines 33-39: has `margin: 0` — keep as delta)
    - templates/sellers/edit.html (lines 29-35: has `margin: 0 0 0.25rem 0` — keep as delta)
    - templates/sellers/list_refined.html (lines 35-41: has `margin: 0` — keep as delta)
    - templates/history/list_refined.html (lines 35-41: has `margin: 0` — keep as delta)
    - templates/clients/list.html (lines 22-29: has `margin-bottom: 0.5rem` — keep as delta)
    - templates/employees/create.html (lines 9-15: has `margin-bottom: 0.5rem` — keep as delta)
    - templates/employees/list.html (lines 21-27: has `margin-bottom: 0.5rem` — keep as delta)
    - templates/services/list.html (lines 21-27: has `margin-bottom: 0.5rem` — keep as delta)
  </read_first>
  <action>
For each template listed in `<files>`, apply the following rules:

**RULE A — Remove entire .page-title block** when the block contains ONLY properties that duplicate the global definition exactly. The global definition provides: `font-family`, `font-size: 1.75rem`, `font-weight: 600`, `letter-spacing: -0.02em`, `color: var(--color-ink)`. If the local block contains ONLY these properties (in any order, with any whitespace), delete the entire block.

Templates where the ENTIRE block can be deleted (no local delta):
- `appointments/calendar.html` (line 9) — only properties: font-family, font-size, font-weight, letter-spacing, margin-bottom: 0.5rem... wait — it HAS margin-bottom: 0.5rem, so keep `{ margin-bottom: 0.5rem; }` as delta
- `appointments/create.html` (line 8) — has margin-bottom: 0.5rem → keep `{ margin-bottom: 0.5rem; }`
- `appointments/edit.html` (line 8) — has margin-bottom: 0.5rem → keep `{ margin-bottom: 0.5rem; }`
- `appointments/list.html` (line 9) — has margin-bottom: 0.5rem → keep `{ margin-bottom: 0.5rem; }`
- `appointments/view.html` (line 9) — has margin-bottom: 0.25rem → keep `{ margin-bottom: 0.25rem; }`
- `auth/change_password.html` (line 9) — has margin-bottom: 2rem → keep `{ margin-bottom: 2rem; }`
- `income/dashboard.html` (line 9) — has margin-bottom: 0.5rem → keep `{ margin-bottom: 0.5rem; }`
- `roles/create.html` (line 8) — has margin-bottom: 2rem → keep `{ margin-bottom: 2rem; }`
- `roles/edit.html` (line 8) — has margin-bottom: 0.5rem → keep `{ margin-bottom: 0.5rem; }`
- `roles/list.html` (line 9) — no margin, no extra props → DELETE ENTIRE BLOCK
- `services/create.html` (line 9) — has margin-bottom: 0.5rem → keep `{ margin-bottom: 0.5rem; }`
- `users/create.html` (line 8) — has margin-bottom: 2rem → keep `{ margin-bottom: 2rem; }`
- `users/edit.html` (line 8) — has margin-bottom: 2rem → keep `{ margin-bottom: 2rem; }`
- `users/list.html` (line 9) — no margin → DELETE ENTIRE BLOCK

Templates with multi-line blocks needing partial reduction to delta-only:
- `appointments/calendar_month.html` (line 9) → replace full block with: `.page-title { font-size: 1.75rem; margin-bottom: 0.25rem; }` — CRITICAL: change 1.375rem to 1.75rem, remove all other props except margin-bottom
- `appointments/calendar_week.html` (line 9) → same as calendar_month: `.page-title { font-size: 1.75rem; margin-bottom: 0.25rem; }` — CRITICAL: change 1.375rem to 1.75rem

Wait — if `font-size: 1.75rem` is the same as the global, there is no need to keep it locally. Correct delta form:
- `appointments/calendar_month.html` → replace with: `.page-title { margin-bottom: 0.25rem; }`
- `appointments/calendar_week.html` → replace with: `.page-title { margin-bottom: 0.25rem; }`

Remaining multi-line blocks — strip down to delta only (keep ONLY non-global properties):
- `clients/create.html` — read file first; if only global props, delete entire block; if has margin-bottom, keep `{ margin-bottom: X; }`
- `clients/edit.html` — same treatment
- `clients/list.html` (has margin-bottom: 0.5rem) → replace block with: `.page-title { margin-bottom: 0.5rem; }`
- `clients/view.html` — read file, apply delta rule
- `dashboard/index.html` (has margin: 0, no font-size override needed) → replace entire block (lines 41-46) with: `.page-title { margin: 0; }`
- `employees/create.html` (has margin-bottom: 0.5rem) → replace block with: `.page-title { margin-bottom: 0.5rem; }`
- `employees/edit.html` — read file, apply delta rule
- `employees/formy_zatrudnienia/list.html` — read file, apply delta rule
- `employees/list.html` (has margin-bottom: 0.5rem) → replace block with: `.page-title { margin-bottom: 0.5rem; }`
- `employees/view.html` — read file, apply delta rule
- `history/list_refined.html` (has margin: 0) → replace block with: `.page-title { margin: 0; }`
- `invoices/create.html` (has margin: 0) → replace block with: `.page-title { margin: 0; }`
- `invoices/edit.html` — read file; has 1.5rem font-size, redundant color; keep only margin delta if any
- `invoices/list_refined.html` (has margin: 0, line-height: 1.2) → replace TOP block (lines 39-47) with: `.page-title { margin: 0; line-height: 1.2; }` — CRITICAL: do NOT touch the media query on line 636 (`@media (max-width: 1024px) { .page-title { font-size: 1.25rem; } }`) — leave it intact
- `invoices/upload.html` (has margin: 0) → replace block with: `.page-title { margin: 0; }`
- `sellers/create.html` — read file, apply delta rule
- `sellers/edit.html` (has margin: 0 0 0.25rem 0) → replace block with: `.page-title { margin: 0 0 0.25rem 0; }`
- `sellers/list_refined.html` (has margin: 0) → replace block with: `.page-title { margin: 0; }`
- `services/edit.html` — read file, apply delta rule
- `services/list.html` (has margin-bottom: 0.5rem) → replace block with: `.page-title { margin-bottom: 0.5rem; }`
- `services/view.html` — read file, apply delta rule
- `settings/email.html` — read file; has 1.5rem, redundant color; keep only margin delta if any

**Delta-only result format** (always single property per block, no redundant declarations):
```html
<style>
    .page-title { margin-bottom: 0.5rem; }
    /* ...other styles... */
</style>
```

**If the .page-title block had ONLY global-matching properties** (no margin, no line-height, no other local delta), delete the entire `.page-title { ... }` block from the `<style>` tag.

**NEVER delete the `<style>` tag itself**, only the `.page-title { }` block inside it. Other classes in the same `<style>` block must remain untouched.
  </action>
  <verify>
    <automated>grep -rn "font-size.*1\.375rem" C:/Projects/faktura_scanner_flask/templates/appointments/calendar_month.html C:/Projects/faktura_scanner_flask/templates/appointments/calendar_week.html</automated>
  </verify>
  <done>
    - `calendar_month.html` and `calendar_week.html` no longer contain `font-size: 1.375rem` in any `.page-title` block
    - Every remaining `.page-title { ... }` block in any template contains ONLY delta properties (margin, line-height, or other non-global-matching values) — no font-size, font-weight, font-family, letter-spacing, or color duplicates
    - `dashboard/index.html` `.page-title` block reduced to `{ margin: 0; }` only
    - `invoices/list_refined.html` media query `@media (max-width: 1024px) { .page-title { font-size: 1.25rem; } }` is still present at line ~636
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip local .page-subtitle blocks from all templates (keep non-standard deltas)</name>
  <files>
    templates/appointments/calendar.html,
    templates/appointments/calendar_month.html,
    templates/appointments/calendar_week.html,
    templates/appointments/create.html,
    templates/appointments/edit.html,
    templates/appointments/list.html,
    templates/appointments/view.html,
    templates/clients/create.html,
    templates/clients/edit.html,
    templates/clients/list.html,
    templates/clients/view.html,
    templates/employees/create.html,
    templates/employees/edit.html,
    templates/employees/formy_zatrudnienia/list.html,
    templates/employees/list.html,
    templates/employees/view.html,
    templates/income/dashboard.html,
    templates/invoices/edit.html,
    templates/roles/edit.html,
    templates/roles/list.html,
    templates/sellers/create.html,
    templates/sellers/edit.html,
    templates/services/create.html,
    templates/services/edit.html,
    templates/services/list.html,
    templates/services/view.html,
    templates/settings/email.html,
    templates/users/list.html
  </files>
  <read_first>
    Read each file before editing. Priority reads (non-standard values that must be KEPT):
    - templates/roles/edit.html (line 9) — has `font-family: monospace` for UUID display — KEEP entire block as local override
    - templates/invoices/edit.html (line 35) — has `font-size: 0.75rem` — KEEP as local override (different from global 0.8125rem)
    - templates/appointments/calendar_month.html (line 10) — has `font-size: 0.75rem` — KEEP as local override
    - templates/appointments/calendar_week.html (line 10) — has `font-size: 0.75rem` — KEEP as local override
    - templates/sellers/create.html (line 34) — has `font-size: 0.875rem` — KEEP as local override
    - templates/sellers/edit.html (line 37) — has `font-size: 0.875rem` — KEEP as local override
    - templates/settings/email.html (line 34) — has `font-size: 0.875rem` — KEEP as local override
  </read_first>
  <action>
The global `.page-subtitle` definition is: `color: var(--color-ink-muted); font-size: 0.8125rem; font-weight: 300;`

**RULE: DELETE entire .page-subtitle block** when it contains ONLY these exact values (any order, any whitespace). These are fully covered by the global.

**RULE: KEEP entire .page-subtitle block** when it contains any non-standard value:
- Different `font-size` (0.75rem or 0.875rem instead of 0.8125rem)
- Any `font-family` (e.g. monospace)
- Any `margin-bottom` or other spacing property (genuine contextual delta)

Apply the rules:

**DELETE entire block** (matches global exactly — no local delta):
- `appointments/calendar.html` (line 10) — `font-size: 0.8125rem` matches → DELETE
- `appointments/create.html` (line 9) — `font-size: 0.8125rem`, has `margin-bottom: 2rem` → this IS a local delta, **KEEP** `{ color: var(--color-ink-muted); font-size: 0.8125rem; font-weight: 300; margin-bottom: 2rem; }` OR reduce to `{ margin-bottom: 2rem; }` (delta only). Use delta-only form: `.page-subtitle { margin-bottom: 2rem; }`
- `appointments/edit.html` (line 9) — same as create.html → `.page-subtitle { margin-bottom: 2rem; }`
- `appointments/list.html` (line 10) — `font-size: 0.8125rem`, no delta → DELETE
- `appointments/view.html` (line 10) — `font-size: 0.8125rem`, no delta → DELETE
- `employees/formy_zatrudnienia/list.html` (line 23) — `font-size: 0.8125rem`, no delta → DELETE
- `income/dashboard.html` (line 10) — `font-size: 0.8125rem`, no delta → DELETE
- `roles/list.html` (line 10) — `font-size: 0.8125rem`, no font-weight declared but global covers it → DELETE
- `services/create.html` (line 10) — `font-size: 0.8125rem`, no delta → DELETE
- `users/list.html` (line 10) — `font-size: 0.8125rem`, no delta → DELETE
- `clients/list.html`, `clients/create.html`, `clients/edit.html`, `clients/view.html` — read each; if font-size is 0.8125rem with no extra delta, DELETE; if has margin or other delta, reduce to delta-only
- `employees/create.html`, `employees/edit.html`, `employees/list.html`, `employees/view.html` — same rule
- `services/edit.html`, `services/list.html`, `services/view.html` — same rule

**KEEP as-is** (non-standard values — intentional local overrides):
- `appointments/calendar_month.html` (line 10, `font-size: 0.75rem`) → KEEP entire block
- `appointments/calendar_week.html` (line 10, `font-size: 0.75rem`) → KEEP entire block
- `invoices/edit.html` (line 35, `font-size: 0.75rem`) → KEEP entire block
- `roles/edit.html` (line 9, `font-family: monospace`) → KEEP entire block
- `sellers/create.html` (line 34, `font-size: 0.875rem`) → KEEP entire block
- `sellers/edit.html` (line 37, `font-size: 0.875rem`) → KEEP entire block
- `settings/email.html` (line 34, `font-size: 0.875rem`) → KEEP entire block

**Reduction rule for multi-line blocks:** When the only non-global property is `margin-bottom`, reduce to `{ margin-bottom: X; }` only. Example: `appointments/create.html` → `.page-subtitle { margin-bottom: 2rem; }`

**NEVER delete the `<style>` tag itself**, only the `.page-subtitle { ... }` block inside it.
  </action>
  <verify>
    <automated>grep -rn "\.page-subtitle\s*{" C:/Projects/faktura_scanner_flask/templates/ --include="*.html" | grep -v "0\.75rem\|0\.875rem\|monospace\|margin-bottom"</automated>
  </verify>
  <done>
    - `grep -rn "\.page-subtitle\s*{" templates/` returns ONLY blocks with non-standard values (0.75rem, 0.875rem, monospace, margin-bottom delta) — no exact-global-duplicate blocks remain
    - `appointments/calendar_month.html` and `calendar_week.html` still have their `font-size: 0.75rem` `.page-subtitle` block (compact calendar context preserved)
    - `roles/edit.html` still has its `font-family: monospace` `.page-subtitle` block (UUID display preserved)
    - `sellers/create.html`, `sellers/edit.html`, `settings/email.html` still have their `font-size: 0.875rem` blocks
    - `appointments/create.html` and `appointments/edit.html` have `.page-subtitle { margin-bottom: 2rem; }` as delta-only (not deleted)
  </done>
</task>

<task type="auto">
  <name>Task 3: Run build verification and grep audit</name>
  <files>static/css/output.css</files>
  <read_first>
    No file to read — this is a verification task only.
  </read_first>
  <action>
Run the build to confirm no template edits introduced any CSS syntax that breaks the Tailwind pipeline:

```bash
cd C:/Projects/faktura_scanner_flask && npm run build:css
```

Then run the cleanup audit to count remaining local redeclarations:

```bash
grep -rn "\.page-title\s*{" C:/Projects/faktura_scanner_flask/templates/ --include="*.html"
grep -rn "\.page-subtitle\s*{" C:/Projects/faktura_scanner_flask/templates/ --include="*.html"
```

Review the grep output. Acceptable remaining lines:
- `.page-title` blocks containing ONLY `margin`, `margin-bottom`, `line-height` — these are legitimate deltas
- `.page-subtitle` blocks with `font-size: 0.75rem`, `font-size: 0.875rem`, `font-family: monospace`, or `margin-bottom` — these are legitimate overrides
- The `@media (max-width: 1024px) { .page-title { font-size: 1.25rem; } }` in `invoices/list_refined.html` — legitimate responsive override

NOT acceptable:
- Any `.page-title` block containing `font-size: 1.375rem` (should be gone from calendar files)
- Any `.page-title` block containing `font-size: 1.5rem` (should be gone from dashboard, history, invoices, sellers templates)
- Any `.page-subtitle` block containing `font-size: 0.8125rem` that duplicates the global exactly (should be deleted)

If any unacceptable blocks remain, fix them before declaring this plan complete.
  </action>
  <verify>
    <automated>cd C:/Projects/faktura_scanner_flask && npm run build:css && echo "BUILD_OK" && grep -rn "font-size.*1\.375rem" templates/ --include="*.html" | wc -l && grep -rn "font-size.*1\.5rem" templates/appointments/ templates/dashboard/ templates/history/ templates/invoices/ templates/sellers/ --include="*.html" | grep "page-title" | wc -l</automated>
  </verify>
  <done>
    - `npm run build:css` exits 0
    - `grep -rn "font-size.*1\.375rem" templates/` returns 0 results (no more 1.375rem .page-title overrides)
    - `grep -rn "font-size.*1\.5rem" templates/*/  | grep "page-title"` returns 0 results (no more 1.5rem .page-title in template directories that had them)
    - All remaining `.page-title` local blocks are delta-only (margin or line-height properties only)
  </done>
</task>

</tasks>

<verification>
Final state check after all tasks:

```bash
# No full .page-title redeclarations (only delta blocks acceptable)
grep -rn "font-size.*1\.375rem" C:/Projects/faktura_scanner_flask/templates/ --include="*.html"
# Expected: 0 results

grep -rn "font-family.*font-display" C:/Projects/faktura_scanner_flask/templates/ --include="*.html" | grep "page-title"
# Expected: 0 results (font-family now comes from global only)

# Build still passes
cd C:/Projects/faktura_scanner_flask && npm run build:css
# Expected: exits 0

# Spot-check that calendar templates still have margin-bottom delta
grep -n "page-title" C:/Projects/faktura_scanner_flask/templates/appointments/calendar_month.html
# Expected: .page-title { margin-bottom: 0.25rem; }

# Confirm invoices/list_refined.html media query preserved
grep -n "page-title" C:/Projects/faktura_scanner_flask/templates/invoices/list_refined.html
# Expected: two lines — delta block (margin: 0) + media query block (font-size: 1.25rem)
```
</verification>

<success_criteria>
- Zero templates contain `font-size: 1.375rem` in a `.page-title` context (TYPO-02 closes for calendar templates)
- Zero templates contain a full `.page-title` redeclaration with `font-size`, `font-weight`, `font-family`, `letter-spacing`, `color` — only delta properties (margin, line-height) remain locally
- Zero templates contain a full `.page-subtitle` redeclaration matching exactly `color: var(--color-ink-muted); font-size: 0.8125rem; font-weight: 300` (TYPO-01)
- Non-standard `.page-subtitle` overrides preserved: 0.75rem in calendar files, 0.875rem in sellers/settings, monospace in roles/edit
- `invoices/list_refined.html` responsive media query for `.page-title` at line ~636 is intact
- `npm run build:css` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-architecture/01-02-SUMMARY.md`
</output>
