---
phase: 03-color-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/users/list.html
  - templates/appointments/calendar.html
  - templates/auth/forgot_password.html
  - templates/auth/login.html
  - templates/auth/reset_password.html
  - templates/errors/404.html
  - templates/errors/500.html
  - templates/invoices/upload.html
  - templates/sellers/create.html
  - templates/sellers/list_refined.html
  - templates/services/create.html
  - templates/services/edit.html
  - templates/settings/email.html
  - templates/clients/create.html
  - templates/clients/edit.html
  - templates/appointments/create.html
autonomous: true
requirements: [COL-01, COL-02]

must_haves:
  truths:
    - "Searching templates for #c9a227 returns zero results (excluding superadmin)"
    - "Searching templates for #d97706 returns zero results (excluding superadmin)"
    - "users/list.html SVG icon renders gold via currentColor inheritance, not hardcoded stroke"
    - "calendar.html coverage bar reads colors from CSS custom properties at runtime"
    - "All 15 files that had background: #333 now use var(--color-ink)"
  artifacts:
    - path: "templates/users/list.html"
      provides: "SVG stroke=currentColor + parent text-brand-500, badge hex replaced with CSS vars"
      contains: "stroke=\"currentColor\""
    - path: "templates/appointments/calendar.html"
      provides: "JS coverage bar using getComputedStyle for all 3 color branches"
      contains: "getPropertyValue"
  key_links:
    - from: "templates/users/list.html"
      to: "static/css/input.css"
      via: "text-brand-500 class inherits --color-accent via Tailwind"
      pattern: "text-brand-500"
    - from: "templates/appointments/calendar.html"
      to: "static/css/input.css"
      via: "getComputedStyle reads --color-status-completed, --color-status-in-progress, --color-status-cancelled"
      pattern: "getPropertyValue.*--color-status"
---

<objective>
Eliminate the two explicit COL-02 hex targets (#c9a227 in users/list.html SVG, #d97706 in calendar.html JS) and batch-replace all `background: #333` button hover patterns across 15 template files with `var(--color-ink)`.

Purpose: This plan addresses the primary COL-02 success criteria ("Searching templates for #c9a227 and #d97706 returns zero results") and eliminates the single largest category of hardcoded hex values (17 occurrences of #333 hover pattern).

Output: 16 template files with hardcoded hex replaced by CSS custom properties or Tailwind tokens. Tailwind build regenerated.
</objective>

<execution_context>
@C:/Users/piotrperesiak/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/piotrperesiak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-color-cleanup/03-CONTEXT.md
@.planning/phases/03-color-cleanup/03-RESEARCH.md

<interfaces>
<!-- Token definitions from input.css :root block -->
From static/css/input.css:
```css
--color-ink: #1a1a1a;
--color-accent: #c9a227;
--color-accent-muted: rgba(201, 162, 39, 0.12);
--color-status-in-progress: #d97706;
--color-status-completed: #2d6a4f;
--color-status-cancelled: #dc2626;
--color-info: #1e6091;
--color-ink-subtle: #8a8a8a;
```

From tailwind.config.js:
```javascript
brand: { 500: '#c9a227' }
// Available classes: text-brand-500, bg-brand-500, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace COL-02 hex targets in users/list.html and calendar.html</name>
  <files>templates/users/list.html, templates/appointments/calendar.html</files>
  <read_first>
    - templates/users/list.html (current state -- SVG at line ~86, badges at lines ~16-19, alerts at lines ~106-107, btn-reset at line ~27, background at line ~85)
    - templates/appointments/calendar.html (current state -- coverage bar JS at lines ~735-739)
    - static/css/input.css (token reference -- :root block lines 1-82)
    - .planning/phases/03-color-cleanup/03-RESEARCH.md (patterns section)
  </read_first>
  <action>
**users/list.html** -- 8 hex replacements:

1. **Line ~85-86 (SVG container + stroke):**
   - Change the parent div: add class `text-brand-500` and replace `background: #fdf6e3` with `background: var(--color-accent-muted)`
   - Change SVG: `stroke="#c9a227"` becomes `stroke="currentColor"`
   - Before: `style="width: 2.5rem; height: 2.5rem; border-radius: 2px; background: #fdf6e3;"` + `stroke="#c9a227"`
   - After: `class="text-brand-500" style="width: 2.5rem; height: 2.5rem; border-radius: 2px; background: var(--color-accent-muted);"` + `stroke="currentColor"`
   - NOTE: `#fdf6e3` is a light gold bg; `var(--color-accent-muted)` is `rgba(201,162,39,0.12)` -- slightly different shade but semantically correct per CONTEXT.md

2. **Line ~16 (.badge-active):** `background: #dcfce7; color: #166534` becomes `background: var(--color-status-confirmed-bg); color: var(--color-success)`

3. **Line ~17 (.badge-inactive):** `background: #f3f4f6` -- no exact token. Replace with `background: var(--color-surface)` (fafafa vs f3f4f6, negligible delta)

4. **Line ~19 (.badge-superuser):** `background: #fef3c7; color: #92400e` becomes `background: var(--color-status-in-progress-bg); color: var(--color-warning)`

5. **Line ~27 (.btn-reset):** `border: 1px solid #fca5a5` -- no border token. Keep hex with comment: `border: 1px solid #fca5a5; /* no border token */`

6. **Line ~106 (reset-pw-error div):** `border: 1px solid #fecaca` -- keep hex with comment: `/* no border token */`

7. **Line ~107 (reset-pw-success div):** `border: 1px solid #bbf7d0` -- keep hex with comment: `/* no border token */`

**calendar.html** -- 3 hex replacements in JS updateCoverage function:

8. **Lines ~735-739 (JS coverage bar):** Replace all 3 hardcoded hex colors with getComputedStyle reads. The function is called at runtime so reading inside the function body is safe:
   ```javascript
   // Replace the three hex lines:
   // '#2d6a4f' -> getComputedStyle read of --color-status-completed
   // '#d97706' -> getComputedStyle read of --color-status-in-progress
   // '#dc2626' -> getComputedStyle read of --color-status-cancelled

   // Add at the top of the if/else block (inside the function, after the null check):
   const root = document.documentElement;
   const style = getComputedStyle(root);
   const colorGreen = style.getPropertyValue('--color-status-completed').trim();
   const colorOrange = style.getPropertyValue('--color-status-in-progress').trim();
   const colorRed = style.getPropertyValue('--color-status-cancelled').trim();

   // Then use colorGreen, colorOrange, colorRed in the three branches
   ```
  </action>
  <verify>
    <automated>grep -n "#c9a227\|#d97706" templates/users/list.html templates/appointments/calendar.html</automated>
  </verify>
  <acceptance_criteria>
    - templates/users/list.html contains `stroke="currentColor"` (grep confirms)
    - templates/users/list.html contains `text-brand-500` (grep confirms)
    - templates/users/list.html contains `var(--color-accent-muted)` (grep confirms)
    - templates/users/list.html does NOT contain `#c9a227` (grep returns empty)
    - templates/users/list.html does NOT contain `#fdf6e3` (grep returns empty)
    - templates/users/list.html contains `var(--color-status-confirmed-bg)` (grep confirms)
    - templates/users/list.html contains `var(--color-success)` (grep confirms)
    - templates/users/list.html contains `var(--color-status-in-progress-bg)` (grep confirms)
    - templates/users/list.html contains `var(--color-warning)` (grep confirms)
    - templates/appointments/calendar.html contains `getPropertyValue('--color-status-completed')` (grep confirms)
    - templates/appointments/calendar.html contains `getPropertyValue('--color-status-in-progress')` (grep confirms)
    - templates/appointments/calendar.html contains `getPropertyValue('--color-status-cancelled')` (grep confirms)
    - templates/appointments/calendar.html does NOT contain `#d97706` (grep returns empty)
    - templates/appointments/calendar.html does NOT contain `'#2d6a4f'` (grep returns empty)
    - templates/appointments/calendar.html does NOT contain `'#dc2626'` (grep returns empty -- note: the CSS var definition in input.css uses this value, but the JS string literal should be gone)
  </acceptance_criteria>
  <done>Both COL-02 explicit targets eliminated. users/list.html SVG uses currentColor + text-brand-500. calendar.html JS reads all 3 coverage colors from CSS custom properties. Zero occurrences of #c9a227 or #d97706 in either file.</done>
</task>

<task type="auto">
  <name>Task 2: Replace #333 hover pattern across 15 templates and handle remaining Plan 01 hex</name>
  <files>
    templates/auth/forgot_password.html,
    templates/auth/login.html,
    templates/auth/reset_password.html,
    templates/errors/404.html,
    templates/errors/500.html,
    templates/invoices/upload.html,
    templates/sellers/create.html,
    templates/sellers/list_refined.html,
    templates/services/create.html,
    templates/services/edit.html,
    templates/settings/email.html,
    templates/clients/create.html,
    templates/clients/edit.html,
    templates/appointments/create.html
  </files>
  <read_first>
    - templates/auth/login.html (standalone auth template -- has #333 at line ~75)
    - templates/auth/forgot_password.html (line ~73)
    - templates/auth/reset_password.html (line ~73)
    - templates/errors/404.html (line ~79)
    - templates/sellers/list_refined.html (line ~61 for #333, line ~811 for #17A2B8)
    - templates/invoices/upload.html (line ~232 for #333, line ~254 for #1f5a3a)
    - templates/services/create.html (line ~21 for #333)
    - static/css/input.css (token reference -- --color-ink: #1a1a1a)
  </read_first>
  <action>
**Bulk #333 replacement (14 files, 15 occurrences):**

In each of the following files, replace `background: #333` with `background: var(--color-ink)`:

| File | Line (approx) | Pattern |
|------|--------------|---------|
| auth/forgot_password.html | 73 | `background: #333;` |
| auth/login.html | 75 | `background: #333;` |
| auth/reset_password.html | 73 | `background: #333;` |
| clients/create.html | 120 | `background: #333;` |
| clients/edit.html | 154 | `background: #333;` |
| errors/404.html | 79 | `background: #333;` |
| errors/500.html | 79 | `background: #333;` |
| invoices/upload.html | 232 | `background: #333;` |
| sellers/create.html | 164 | `background: #333;` |
| sellers/list_refined.html | 61 | `background: #333;` |
| services/create.html | 21 | `background: #333;` |
| services/edit.html | 137 | `background: #333;` |
| settings/email.html | 148 | `background: #333;` |
| appointments/create.html | 33 | `background: #333;` |

Visual note: `#333` (rgb 51,51,51) vs `var(--color-ink)` (#1a1a1a = rgb 26,26,26) is a minor darkening on button hover state. Acceptable per RESEARCH.md Pitfall 3 analysis.

**Additional hex in Plan 01 files:**

15. **sellers/list_refined.html line ~811:** `color: #17A2B8` -- teal info badge. No exact token (`--color-info` is #1e6091, a different blue). Leave hex with comment: `color: #17A2B8; /* intentional: info badge teal, no matching token */`

16. **invoices/upload.html line ~254:** `background: #1f5a3a` -- dark green button. No exact token (`--color-success` is #2d6a4f, lighter). Leave hex with comment: `background: #1f5a3a; /* no token -- intentional dark green */`

**After all replacements:** Run `npm run build` to regenerate output.css (needed because Task 1 added `text-brand-500` class to users/list.html).
  </action>
  <verify>
    <automated>grep -rn "background: #333" templates/ --include="*.html" | grep -v superadmin_edit</automated>
  </verify>
  <acceptance_criteria>
    - grep for `background: #333` across all templates (excluding superadmin) returns zero results
    - templates/auth/login.html contains `background: var(--color-ink)` (grep confirms)
    - templates/auth/forgot_password.html contains `background: var(--color-ink)` (grep confirms)
    - templates/errors/404.html contains `background: var(--color-ink)` (grep confirms)
    - templates/sellers/list_refined.html contains `/* intentional: info badge teal` (grep confirms)
    - templates/invoices/upload.html contains `/* no token` (grep confirms)
    - `npm run build` exits with code 0
    - grep for `#c9a227` and `#d97706` across ALL templates (excluding superadmin) returns zero results (full COL-02 gate check)
  </acceptance_criteria>
  <done>All 15 occurrences of `background: #333` replaced with `var(--color-ink)`. Two no-token hex values documented with intentional comments. Tailwind build regenerated successfully. Combined with Task 1, zero #c9a227 and #d97706 remain in any in-scope template.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **COL-02 gate (primary):**
   ```bash
   grep -rn "#c9a227\|#d97706" templates/ --include="*.html" | grep -v superadmin_edit
   ```
   Must return empty (zero results).

2. **#333 elimination:**
   ```bash
   grep -rn "background: #333" templates/ --include="*.html" | grep -v superadmin_edit
   ```
   Must return empty.

3. **Build integrity:**
   ```bash
   npm run build
   ```
   Must exit 0.

4. **Hex reduction count:**
   ```bash
   grep -rn '#[0-9a-fA-F]\{6\}' templates/ --include="*.html" | grep -v superadmin_edit | grep -v '&#' | wc -l
   ```
   Should be reduced by ~20 from baseline (was ~57, target ~37 or less after this plan).
</verification>

<success_criteria>
- Zero occurrences of #c9a227 in templates (excluding superadmin)
- Zero occurrences of #d97706 in templates (excluding superadmin)
- Zero occurrences of background: #333 in templates (excluding superadmin)
- users/list.html SVG uses stroke="currentColor" with parent text-brand-500
- calendar.html JS coverage bar reads all 3 colors from CSS custom properties
- npm run build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-color-cleanup/03-01-SUMMARY.md`
</output>
