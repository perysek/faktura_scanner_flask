#TASK: implement new feature of absence balance tracking.
#SPECS:
## absence categories management module db datamodel (db table, alembic migration needed?) with new fields: 'is_tracked' (bool) - when true the absence is being tracked, 'count_period' - for how long the counter is ticking before reset to 0, 'resets_at' - day number (x of ~365 days of the year) when the counter resets to 0...

+ other fields at your decision + FKs to other tables according your analysis and insight about implementation (for example - each employee would have defined/adjustable allowed limit of count value per absence category

## all required validation checks on db updates, check values vs. defined limits on each value change trigger, absence request submit must have constraint checks for counters dangerously reaching/exceeding defined limits (i.e. on absence request submission the warning or blocking error prevents from outreaching max allowed value in case potentially the request would be approved).

## GUI should ensure the current data, progress, status are displayed on the page views which are suitable for including new data along existing informatioin/data being displayed (i.e. current day-offs count displayed in the employees table view in new added column; current counts vs limits set (shown in employee view page as progress-bar; and in employee edit page - adjustable limit value input field - can be different display solution if you decide and think of better/more attractive option/idea). Be creative, unique but also do not drift away from currently used GUI design patterns. Feel free to add new GUI components.

## toast notification for users in case counts are getting close to the limits

## management of balances, limits, on/off options should ensure full adjustability and flexibility of CRUD operations on new added database columns/tables/FKs

## ensure GUI/UX allows user to see full audit trails with all details of db updates/changes/events related to new-added data/features.

## for essential db tables values updates/changes - ensure user confirmation custom modals are displayed

## ensure soft-delete is applied for delete operations

## ensure new addeed features and displays/templates have accessible navlinks in sidebar and within page views if needed.
## ensure convinient tabbing / trapps to prevent tabbing escapes for opitmal UX
## ensure esc keybingings for navigating to previous page views and ensure view states handling for back-navigating

### ask clarifying question if approach choices are multiple and human decision is recommended


 